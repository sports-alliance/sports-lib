import { DataNumber } from './data.number';

export class DataGroundContactTimeMin extends DataNumber {
  static type = 'Minimum Ground Contact Time';
  static aliases = ['Ground Contact Time Min'];
  static unit = 'ms';
}
