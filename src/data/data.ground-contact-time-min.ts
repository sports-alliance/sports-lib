import { DataGroundContactTime } from './data.ground-contact-time';

export class DataGroundContactTimeMin extends DataGroundContactTime {
  static type = 'Minimum Ground Contact Time';
  static aliases = ['Ground Contact Time Min'];
  static unit = 'ms';
}
