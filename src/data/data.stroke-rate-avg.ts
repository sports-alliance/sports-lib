import { DataStrokeRate } from './data.stroke-rate';

/** Average stroke or paddle cycles per minute. */
export class DataStrokeRateAvg extends DataStrokeRate {
  static type = 'Average Stroke Rate';
}
