import { DataStrokeRate } from './data.stroke-rate';

/** Maximum stroke or paddle cycles per minute. */
export class DataStrokeRateMax extends DataStrokeRate {
  static type = 'Maximum Stroke Rate';
}
