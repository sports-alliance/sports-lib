import { DataStrokeRate } from './data.stroke-rate';

/** Minimum stroke or paddle cycles per minute. */
export class DataStrokeRateMin extends DataStrokeRate {
  static type = 'Minimum Stroke Rate';
}
